services:
  app:
    build: .
    image: ivy-league-solutions:latest
    container_name: ivy-league-solutions-app
    restart: unless-stopped
    ports:
      - "3002:3000"
    env_file:
      - .env.local
